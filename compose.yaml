name: pyemchargeonsolar

services:
  pyemchargeonsolar:
    image: vorezal/pyemchargeonsolar:latest
    env_file:
      - "./.env"
    volumes:
      - "./.credentials:/app/.credentials"
      - "./volume:/app/volume"
    restart: unless-stopped
